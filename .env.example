# ═══════════════════════════════════════════════════════════════
# ClawPTT — Zello Voice Bridge for OpenClaw
#
# ClawPTT handles voice I/O only: STT, TTS, and Zello transport.
# All intelligence (model, tools, search, persona) is configured
# in the agent that sits behind the LLM endpoint.
# ═══════════════════════════════════════════════════════════════

# ─── Zello Work ────────────────────────────────────────────────
ZELLO_NETWORK=your-network-name
ZELLO_BOT_USER=bot-username
ZELLO_BOT_PASS=bot-password
ZELLO_BRIDGE_CHANNELS=channel-name   # comma-separated

# ─── Zello REST API (admin/data — runs in-process with bridge) ─
# Set ZELLO_API_KEY to enable the API server. Without it, only the voice bridge runs.
# ZELLO_API_KEY=your-api-key           # from Zello admin console → Settings → API
# ZELLO_ADMIN_USER=admin               # falls back to ZELLO_BOT_USER
# ZELLO_ADMIN_PASS=admin-password      # falls back to ZELLO_BOT_PASS
# CLAWPTT_API_PORT=18790               # default 18790
# CLAWPTT_API_TOKEN=                   # default: same as GATEWAY_TOKEN

# ─── LLM Endpoint ─────────────────────────────────────────────
# "openclaw" → OpenClaw gateway (agent handles model/tools/search)
# "local"    → any OpenAI-compatible endpoint (vLLM, Ollama, LiteLLM)
LLM_BACKEND=openclaw

# OpenClaw gateway (default)
GATEWAY_TOKEN=your-gateway-token
OPENCLAW_GATEWAY=http://127.0.0.1:18789
OPENCLAW_AGENT=main

# Local endpoint (when LLM_BACKEND=local)
# LOCAL_LLM_URL=http://127.0.0.1:8888/v1/chat/completions
# LOCAL_LLM_API_KEY=your-key
# LOCAL_LLM_MODEL=your-model-id
# LOCAL_LLM_MAX_TOKENS=512
# LOCAL_LLM_TEMPERATURE=0.7
# AGENT_SYSTEM_PROMPT="Your system prompt here"

# ─── STT ───────────────────────────────────────────────────────
WHISPER_MODEL=base.en                # base.en, small.en, medium.en, large-v3
# STT_METHOD=faster-whisper          # or "zello-transcription"
# VENV_PYTHON=python3                # path to python with faster-whisper + sherpa-onnx

# ─── TTS ───────────────────────────────────────────────────────
TTS_VOICE=en_US-lessac-high          # Piper voice model name
# SHERPA_ONNX_DIR=~/.clawptt/tts   # where voice models are stored
# Models: https://github.com/k2-fsa/sherpa-onnx/releases/tag/tts-models
# Voices: en_US-lessac-high, en_US-hfc_male-medium, en_GB-alan-medium, etc.
